# Test-only dependencies.
#
# Deliberately does not pull in server/requirements.txt: the unit suite covers
# the typed-decision layer and the classifiers around it, none of which need
# Composio (whose transitive `pysher` dependency needs a compiler).
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Runtime packages the tested modules import.
pydantic>=2.7.0
httpx>=0.27.0
# httpx2 arrives with typesafe-sdk; named explicitly because the test fixtures
# import it directly for MockTransport.
httpx2>=2.0.0
typesafe-sdk>=0.7.0
fastapi>=0.115.0
python-dateutil>=2.9.0
beautifulsoup4>=4.12.0
